/*
 * COMPARABLE
 * - Comparable fornece uma única sequência de ordenação. EM outras palavras, podemos ordenar a coleção com base em um único elemento, como id, nome, preço;
 * - Comparable afeta a classe original, ou seja, a classe atual é modificada;
 * - Comparable fornece o método compareTo() para ordenar elementos
 * - Comparable está presente no pacote java.lang
 * - Podemos ordenar os elementos da lista do tipo Comparable usando o método Collections.sort(List)
 * 
 * COMPARATOR
 * - O Comparador fornece um método compare() para ordenar elementos.
 * - O comparador fornece múltiplas sequências de ordenação. Em outras palavras, podemos ordenar a coleção com base em múltiplos elementos, como id, nome, preço etc.
 * - O comparador não afeta a classe original, ou seja, a classe atual não é modificada;
 * - O comparador está presente em pacote java.util
 * - Podemos ordenas os elementos da lista do tipo Comparador usando o méodo Collections.sort(List, Comparador)
 * 
 * COLLECTIONS
 * - A classe Collections é uma classe utilitária do java para operações comuns em coleções
 * - Ela fornece métodos para ordenação, busca, manipulação e sincronização de coleções
 * - O método sort() é usado para ordenar uma lista em ordem ascendente.
 * - O método sort() em conjunto com Collections.reverseOrder() permite ordenar em oredem decrescente
 */

